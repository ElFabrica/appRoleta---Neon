import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  header: {
    position: "relative",
    width: "100%",
  },
  iconHeader: {
    position: "absolute",
    right: 0,
  },
});
